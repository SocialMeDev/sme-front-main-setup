export default function walletFormatParser(number = 0) {
  if (number % 1 === 0) {
    const newNumber = number * 1000000

    return newNumber
  } else {
    const newNumber = number * 1000000
    return newNumber
  }
}
